apiVersion: v1
kind: Service
metadata:
  name: webserver-rs
  namespace: argocd
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: webserver-rs
