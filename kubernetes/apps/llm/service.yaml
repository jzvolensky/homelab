apiVersion: v1
kind: Service
metadata:
    name: llama-server
    namespace: llm
spec:
    type: ClusterIP
    ports:
        - port: 80
          targetPort: 8080
          protocol: TCP
    selector:
        app: llama-server
