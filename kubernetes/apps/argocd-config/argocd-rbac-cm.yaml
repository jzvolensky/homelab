apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  # GitHub user jzvolensky -> full admin. Everyone else authenticated -> read-only.
  policy.csv: |
    g, jzvolensky, role:admin
  policy.default: role:readonly
