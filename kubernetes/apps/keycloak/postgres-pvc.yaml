apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgres-data
    namespace: keycloak
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: local-path
    resources:
        requests:
            storage: 8Gi
